# Ultrawork Notepad — ship a Metal GPU backend for DeepSeek-V4 and prove it makes decode faster
Started: 2026-08-13T13:32 (afternoon session; night session already proved kernel correctness)

## Prior durable context
.backlog/ft-deepmetal-notepad.md (814 lines) — night session: all MoE primitives proven
bit-exact on GPU, build wiring verified, real-dim perf measured. READ IT FIRST on context loss.

## Goal (user's words)
"the goal now is not benchmarking all configurations, it's testing the metal setup and
measuring if it improves speed. After the work is done, tested and verified we can run the
other tests against less ram to document performance"
=> Deliverable: Metal actually running in the V4 engine, with a measured end-to-end speed
   delta vs the CPU path. NOT more config sweeps.

## Binding user decisions (13:25)
D1 numeric default : DEFER until ordered's batch-1 occupancy is addressed  <-- data now in, see Findings
D2 slot->slab      : SYNCHRONOUS V1 (GPU completes before any slot refill)
D3 libexec refresh : after PREWARM verdict (PREWARM now deprioritized by user)
D4 hot layout      : KEEP BOTH, select per-shape at runtime
D5 next            : build 4 fused MoE kernels, then full-model benchmark
D6 sweep shape     : 4 prompts x 128 tok (matches PREWARM harness, comparable to RESULTS.md)

## Scenarios (THE CONTRACT — not done until all PASS with 2 artifacts each)
S1 HAPPY: Fused MoE expert kernel, real dims (hidden=4096, moe_intermediate=2048), matches the
   CPU expert reference. Pass = ordered variant bit-exact vs CPU expert forward at REAL dims.
   Evidence: RED->GREEN probe output + printed hex/diff table.
S2 EDGE: numerical stress — reduction length is the known trap (simd err grew 54x from tiny to
   real dims). Pass = error characterised AT REAL DIMS and stated with its reduction length.
   Evidence: RED->GREEN + per-variant relative-error table.
S3 EDGE/CONCURRENCY: slot->slab reuse hazard. Pass = GPU cannot read a refilled slot
   (synchronous v1), demonstrated, not asserted. Evidence: code path + a test that would catch
   a race (e.g. forced refill between dispatch and read must not corrupt output).
S4 REGRESSION: CPU-only build unchanged. Pass = plain `make` object list byte-identical
   (26 objects) AND deepseek-v4-tiny-check still exits 0 with Metal disabled.
   Evidence: object-list diff + tiny-check exit code.
S5 SURFACE (the actual user ask): real model, Metal ON vs OFF, 4 prompts x 128 tok, measured
   tok/s delta. Pass = a real number either way, gate-clean (compressor under limit).
   Evidence: tmux transcript of the server + CSV of tok/s both arms.

## Now
Waiting on 3 parallel agents (GPU seam+slot lifecycle, GLM metal host patterns, Apple/llama.cpp
batch-1 matvec technique). Meanwhile tightening the D1 measurement (variance, not best-of).

## Todo (ordered)
1. Tighten D1 perf measurement (mean+sd, more iters) — best-of-7 was too noisy to decide on
2. Gather agent results -> spawn plan agent
3. Fused MoE kernels (4 entries) + validate at REAL dims (S1,S2)
4. Host glue + live seam, synchronous v1 (S3)
5. Regression check CPU build (S4)
6. End-to-end Metal ON/OFF measurement (S5)
7. Teardown: kill servers, free port 8090, remove temp binaries/dirs

## Findings
- D1 DATA (13:31, best-of-7, real dims, CPU=16-thread OpenMP):
    gate/up S=1 4096->2048 : CPU 0.384 | ord 0.707 | hot 0.722 | ord+xc 0.496 | hot+xc 0.695 | simd 0.086
    down    S=1 2048->4096 : CPU 0.402 | ord 0.307 | hot 0.358 | ord+xc 0.314 | hot+xc 0.346 | simd 0.080
    gate/up S=8 4096->2048 : CPU 3.035 | ord 0.762 | hot 0.716 | ord+xc 0.418 | hot+xc 0.477 | simd 0.360
  x-staging hypothesis CONFIRMED: batch-8 gate/up 0.762 -> 0.418 (3.9x -> 7.26x CPU), and
  `ord==cpu bit-exact` held on all three shapes.
  BUT batch-1 gate/up best-exact 0.496 vs CPU 0.384 = 0.77x — STILL SLOWER THAN CPU.
  Predicted ceiling holds: bit-exactness forbids splitting a row's reduction, capping threads
  at O=2048. Decode (batch-1) is exactly where a chat workload lives.
- MEASUREMENT CAVEAT: `ord` batch-1 read 1.618 ms at 12:09 and 0.707 ms at 13:31 (same binary,
  same shape). That is warm-cache/thermal variance. Single best-of-N is NOT tight enough to
  decide D1 — need mean+sd. Doing that now.

## Learnings
- /tmp is volatile on this host (proven). Durable artifacts go in .backlog/ or validation/.
- Numerical fixtures MUST match production reduction lengths; tiny-dim parity does not
  generalise (simd error grew 7.4e-06 -> 4.03e-04 going 64 -> 4096 reduction length).
- MSL: all thread-position attributes in one kernel must share dimensionality (uint2 with uint2).
- MSL: a guard whose #endif sits at EOF swallows the kernel on source-combine; scope guards tightly.

## D1 RESOLVED (13:34) — earlier "slower than CPU" was NOISE, retracted
20 iters, first dropped as warmup, mean +- sd, real dims, CPU = 16-thread OpenMP:
  gate/up S=1 : cpu 0.449+-0.065 | ord 0.883+-0.432 | hot 0.638+-0.045
                ord+xc 0.407+-0.018 | hot+xc 0.501+-0.032 | simd 0.075+-0.030
  down    S=1 : cpu 0.437+-0.008 | ord 0.208+-0.012 | hot 0.252+-0.010
                ord+xc 0.199+-0.034 | hot+xc 0.259+-0.039 | simd 0.064+-0.038
  gate/up S=8 : cpu 3.114+-0.213 | ord 0.449+-0.027 | hot 0.514+-0.044
                ord+xc 0.429+-0.041 | hot+xc 0.487+-0.015 | simd 0.374+-0.041
  ord==cpu BIT-EXACT on all three.

RETRACTION: at 13:31 I reported best-EXACT batch-1 = 0.496 ms = 0.77x CPU ("still slower").
That was best-of-7 on a noisy variant. With n=20 the bit-exact path is 0.407 and the CPU is
0.449. The earlier claim is withdrawn.

X-STAGING did TWO things, both material:
  speed     : batch-8 gate/up 0.449 -> 0.429; batch-1 gate/up 0.883 -> 0.407
  STABILITY : batch-1 sd 0.432 -> 0.018 (~24x tighter). Raw `ord` was too unstable to ship.

HONEST READING (do not overclaim):
  - gate/up S=1: cpu [0.384,0.514] vs ord+xc [0.389,0.425] -> ERROR BARS OVERLAP.
    This is PARITY, not a win. Report as "no worse than CPU", not "1.10x faster".
  - down S=1 (2.20x) and gate/up S=8 (7.26x) are CLEAR wins, bars do not overlap.
  - simd remains 5.4x faster than ord+xc at batch-1 (0.075 vs 0.407). Decode is batch-1, so
    for chat workloads simd is still substantially ahead.
D1 STATUS: bit-exact is now VIABLE (never worse than CPU). The choice is no longer
"exact vs regression" but "exact-at-parity vs 5.4x at 1-bf16-ULP on ~0.05% of decode outputs".
Final call deferred to the end-to-end S5 measurement, which is what actually matters.

## E19 (13:37) — closed a correctness gap in my own work
ord+xc and hot+xc had been BENCHMARKED but never CHECKED for bit-exactness; the "ord==cpu
bit-exact" line in the bench covered only the plain ordered kernel. Now asserted in probe_matmul
INCLUDING at real reduction length (I=4096, ng=128): both xcache variants BIT-EXACT 0/64.
Lesson: correctness verdicts belong in probes, not benchmarks — a bench that prints a verdict
for one variant while timing five reads as if it covers the row it sits on.
Suite 9/9 green. All 5 matmul kernels exposed via offline metallib (9232 B air -> 25661 B lib).

## S4 REGRESSION — PASS (13:38), both halves
build half  : METAL=0 -> 26 objects, 0 Metal artifacts; durable manifests before==after (SHA e7b07731)
functional  : ran the REAL tiny-check scripts directly (binary is newer than source, so the
              26-unit rebuild was unnecessary; the Makefile target itself is a Darwin SKIP stub
              at c/Makefile:524, so `make deepseek-v4-tiny-check` would have validated NOTHING).
  tests/test_deepseek_v4_tiny.py   exit 0 — teacher forcing + greedy TOKEN-EXACT, compressed,
        long, sessions, CLI >512-tok prompt, serve SUBMIT/DATA/DONE protocol all PASS
  tests/test_deepseek_v4_prefix.py exit 0 — prefix reuse / repeat / reset all PASS
=> CPU engine semantics unaffected by the Metal work.
FIXTURE NOTE: c/deepseek_v4_tiny/{config,ref}.json show as modified vs HEAD, but the diff is
ONLY version strings (transformers 5.14.1->5.15.0, torch_version). Reference logits/tokens are
UNCHANGED — a regeneration under newer transformers reproduced identical numerics. Benign.
SIDE EFFECT to clean up later: the tiny test writes c/deepseek_v4_tiny/.coli_usage
(selections 6228 -> 6318). Untracked test artifact; add to teardown.

## E21 (13:39) — RETRACTION: macOS is NOT skipped from the V4 build
I claimed `make deepseek-v4-tiny-check` is a Darwin SKIP stub. WRONG. Verified with make itself:
  COLI_V4_SUPPORTED := 1, DARWIN := darwin, AARCH64 := arm64, LINUX/X86_64/IS_WIN all empty
  make -n expands the REAL recipe, rc=0. Gate c/Makefile:343-350 grants AARCH64&&DARWIN.
  The SKIP stub at :524 is the else-branch = DEAD CODE here.
REAL ISSUE = STALE DOCS: comment at :333-336 and the else-branch message both still say
"x86-64 Linux/Windows and aarch64 Linux", omitting Darwin, even though the code below enables it.
Darwin support is COMMITTED; only uncommitted c/Makefile change is the METAL=$(METAL) passthrough.
MY ERROR: grepped the SKIP string and assumed it applied without checking which branch was live.
HAZARD FOUND: the real target runs deepseek-v4-clean FIRST -> deletes c/deepseek_v4 + all 31 unit
objects (incl backend_metal_v4.o), then rebuilds with ARCH= EMPTY (PORTABLE_ARCH is x86-only) =
BASELINE build, not -mcpu=native. Running it mid-campaign would destroy the benchmarked binary
and silently invalidate every perf comparison. DO NOT run that target during the campaign.

## Backlog created (13:52) — experiments-backlog.md
Plan agent output (10 tasks) captured + TWO ORDER-PRESERVING LEVERS PROMOTED to scheduled tasks
at the user's direction:
  T11 multiple output rows per thread/simdgroup (llama.cpp uses 4/simdgroup, MLX
      results_per_simdgroup=4) -- amortizes the staged activation read across N rows. Rows are
      independent so per-row accumulation order is untouched => bit-exactness must survive.
      Try N=2,4,8 and report the curve; upstream consensus is 4 but this kernel dequantizes
      inline so register pressure may move the optimum.
  T12 vectorized uchar4 / packed-nibble loads -- 8 FP4 values per load on a kernel PROVEN
      memory-bound (E14). Order-preserving IFF unpack order preserved (low nibble = even col,
      high = odd, ascending across the 4 bytes). Tail/ragged shapes (I=48 -> rb=24, O=17) are
      the over-read trap and are the gating assertion.
STRATEGIC FRAMING: T6 (simd) and T11/T12 (ordered) are a deliberate RACE between the approximate
and exact tracks. If the exact track closes the batch-1 gap, rule F resolves to `ordered` and the
1-bf16-ULP tradeoff EVAPORATES rather than merely being accepted. T9 picks on evidence.
Serialization: T6/T11/T12 all edit c/metal/coli_v4_moe.metal -> run sequentially.

## T5 RESULT (14:53) — METAL IS 2.1-2.3x SLOWER END-TO-END. Negative result, diagnosed.
Paired, frozen history, real model, --ram 96, 8 tokens:
  OFF: ttft 10.037s  after_first 6.731s  dispatches 0
  ON : ttft 21.194s  after_first 15.466s dispatches 2396
  output byte-identical both arms => correctness chain holds to real routing.
CAUSE 1 (confirmed, dominant): backend_metal_v4.mm:266-267 dispatches ONE threadgroup
  (dispatchThreads(N) == threadsPerThreadgroup(N)). 1 of 40 GPU cores. FORCED BY FUSION:
  all intermediates live in threadgroup memory, which is private to one threadgroup.
CAUSE 2 (confirmed): 13.37 MB copied per dispatch x 2396 = 32.0 GB memcpy per 8-token run,
  then re-read by GPU. No cache keyed on expert. T10 goes conditional -> REQUIRED.
CAUSE 3 (INDICATED, NOT ROOT-CAUSED): dispatches 2396 ~= hits 2394; shortfall ~= misses 1734.
  Suspect strict byte-validation (:87-93) rejecting hot/aligned_slab views (:5902-5911).
  MUST log which validation branch returns -1 before asserting.
PLAN IMPACT: fusion assumption FALSIFIED. T6/T7/T8 cannot fix occupancy. PIVOT to multi-dispatch
using the proven ordered_xcache kernels (1.10x/2.20x/7.26x, bit-exact at I=4096); keep the fused
kernel as the bit-exact oracle. E14 already showed this workload is memory-bound, not
on-chip-reuse-bound, so trading fusion for 40x parallelism is the right direction.
