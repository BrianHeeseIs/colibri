# Ultrawork Notepad — Wave 2 decode critical-path trace (T4-T15)
Started: 2026-08-30T16:42:12Z

## Plan (exhaustive, atomic)
## Scenarios (the contract)
## Now (single step in progress)
## Todo (remaining, ordered)
## Findings (non-obvious facts with file:line refs)
## Learnings (patterns / pitfalls for next turn)

## Findings (Wave 2 pre-flight, verified from disk 20260830)
- BUILD IS AMALGAMATED N-WAY. c/Makefile.deepseek-v4:141 `$(CC) $(CFLAGS) -D$* -c deepseek_v4.c -o $@`
  -> same .c compiled ~26x, one -DCOLI_V4_UNIT_* each. Prologue (deepseek_v4.c:1-80) is in EVERY object.
  Consequence: `static` storage in decode_trace.h => 26 private copies => report reads untouched copy
  => ALL ZEROS, plausible-looking, and would FALSELY SATISFY the <3% expert_wait kill gate.
- PRECEDENT (prefill trace): storage `static` in ONE unit (UNIT_ST, deepseek_v4.c:105-289), accessors
  NON-static (add:132, mode:127, now_ns:120, report:201), consumers `extern` re-declare
  (3753-3755 BLOCK_HYBRID, 7850-7852 EXPERT_STORE_HOT_ROWS16, 9621-9626 GENERATE_STATS).
  Runtime profiler same shape: coli_v4_profile[] static at 9618, coli_v4_profile_add non-static at 9638.
- T5 TARGET IS LIVE (initially feared dead). deepseek_v4.c:3723-3728 SELF-DEFINES
  COLI_V4_EXPERIMENTAL_DUAL_EXPERT_LOADER when none of DISABLE_DUAL / DISABLE_PERSISTENT /
  EXPERIMENTAL_SYNC are set. Makefile sets none => dual loader IS the default =>
  dual_expert_load_start(4482) / dual_expert_load_finish(4515) are compiled and hot.
- EXISTING expert_wait IS THE SUM OF BOTH HALVES: profiled_expert_load_start:4688 and
  profiled_expert_load_finish:4704 both add to COLI_V4_PROFILE_EXPERT_WAIT. The trace decomposes
  exactly this quantity, so the two must reconcile.
- Loader region lives in UNIT_BLOCK_HYBRID (3715-6243); report site is UNIT_GENERATE_STATS
  (coli_v4_profile_report:9667, called at 11424). Cross-object bridge is therefore MANDATORY.
- ORACLE VERDICT (ses_fac71bd01ffeF1TO0ULjbHjBhn): design D — ONE mode-switched header.
  Always expose types+enum+_Static_assert. Gate storage/local helpers to owner mode; expose only
  extern bridge prototypes in decl-only mode. Non-owner TU then has NO local adder symbol to call
  => wrong call is a COMPILE ERROR, not silent loss. Bridge names: coli_v4_decode_trace_note /
  _enabled / _clock_ns / _report. Local/test names get _local_ infix + poison macros.
  LTO cannot merge distinct internal-linkage arrays, so a wrong design stays wrong (does not
  accidentally work); but private dead writes may be optimised away - do not rely on them.
  _Static_assert on sizeof(names) DOES count as a use (suppresses -Wunused-variable), but must not
  be the load-bearing reason.
  Centralize env parsing in the owner bridge - per-TU getenv would let consumers and owner disagree.
  EXECUTION PROOF: print `decode_trace_total_calls=<sum of stage.calls>` under the v4_profile prefix;
  OFF run => absent/0, ON run => >0. Proves linkage AND execution, not merely that report ran.
  Named residual risks: (a) all-zero may mean path not exercised rather than linkage bug — smoke
  must use a known decode workload; (b) split enable state; (c) self-perturbation in ultra-hot paths.
- STANDALONE TEST MUST KEEP COMPILING UNCHANGED: c/tests/test_decode_trace.c includes the header
  directly and touches coli_v4_decode_trace_stages/_add/_reset. => default (no macro) must be
  IMPL mode; deepseek_v4.c prologue defines DECLS_ONLY for every unit except GENERATE_STATS.

## Learnings — NEW LANDMINE (discovered T0b, 20260830)
- `make -C c check` BEGINS WITH `$(MAKE) clean` (c/Makefile `check:` target = clean; portable; test).
  It DELETED c/deepseek_v4 AND c/tests/test_decode_trace mid-session.
  => ORDERING RULE: run `make -C c check` BEFORE the METAL=1 engine build, never after.
     Running it at T9 after building would destroy the instrumented binary and any nm evidence.
  => After any `make check`, ALWAYS rebuild:
     make -C c -f Makefile.deepseek-v4 METAL=1 deepseek-v4 -j8
     and re-verify `nm c/deepseek_v4 | grep -c coli_v4_metal_expert_forward_batch` > 0.
- `make -C c check` ABORTS at the pre-existing test_fp8_passthrough link failure (rc=2) during the
  test BUILD phase, so it never reaches test_decode_trace. The check target therefore does NOT
  cover our unit tests; they must be built and run EXPLICITLY. Baseline captured:
  .backlog/lab/e130_pre_check.log, failure = undefined _coli_fp8_minprod_enabled, arm64 link, rc=2.
  Post-change comparison is therefore "aborts at the SAME point", not "same full failure set".
- Pre-state binary md5 (now deleted, rebuildable): d92a266b3117953ccad48a74a521c499, metal syms = 1.

## Wave 2+3 COMPLETE (T4-T9), all gates green
Commits: 9606dbb (T1/T2 header split + link test), adc86d5 (T4 owner), 4883574 (T5 wait),
9175ee7 (T6 store), 77f7a85 (T7 omp), 962b59a (T8 control).
T9 static gate: build 0 err/0 warn; metal seam=1; 4/4 unit tests pass; prefix contract
6 compliant / 0 violations; PROFILE_COUNT still 16; coli_v4_profile_report BYTE-IDENTICAL
to eb3b7ea; exactly 1 getenv; storage owned by exactly ONE object (GENERATE_STATS).
make check aborts at the SAME pre-existing test_fp8_passthrough with an IDENTICAL error set.

### Corrections made to the inherited plan (line evidence contradicted it)
- T6 target was NOT coli_expert_lookup: that is a static inline vtable wrapper in
  expert_store.h:76. The shipping implementation is lookup_hot, bound by
  coli_deepseek_v4_expert_store_open via the hot_operations vtable. Instrumenting the
  plain `lookup` beside it would have produced an empty store table.
- T7 hit a real linkage hazard the plan did not foresee: test_hc_omp and test_sparse_omp
  compile deepseek_v4.c with ONE unit macro and link only that object, so instrumenting
  MATH / SPARSE_ATTENTION gave undefined bridge symbols. Fixed with
  c/tests/decode_trace_owner_stub.c added to those two link lines only.
- Oracle's `_local_` rename was DROPPED: it would have forced an edit to the frozen
  test_decode_trace.c. Owner branch kept character-identical instead.

## Next: Wave 4 is ENGINE-EXCLUSIVE and needs operator approval (AGENTS.md constraint 10)
