# Ultrawork Notepad — make deepseek-v4-flash MUCH faster on this M3 Max
Started: 2026-08-13T20:07

## Mandate (user, verbatim intent)
Deep research on max performance: (1) Metal kernels to hardware ceiling, (2) model loading +
cache warming, (3) creative/clever anything. Document ALL theoretical approaches in
performance-boost-research.md. Verify with Oracle + fitting reviewers, iterate to PASS.
Then: pick LEAST-WORK method -> branch ft-[method_name] FROM ft-deepmetal -> build+test.
Repeat over all methods. Base commits pushed first (DONE: fork/ft-deepmetal @ eabb24a).

## Baseline (all paired, frozen-history, this machine)
decode  --ram 96, 8 tok : CPU 6.560-6.980s gen (2.809-2.919 ms/expert), 0.33 tok/s-ish
prefill 799 tok         : 708s CPU = 0.89 s/token prefill, 1.13 tok/s
--ram 64 4-prompt mean  : 0.1815 tok/s (engine sha f84813a9)
Metal best              : 1.118x SLOWER decode, 1.063x slower prefill (S=1 seam, structural)
matmul microbench (n=20): ord+xc 1.10x/2.20x/7.26x vs CPU at S=1/S=1-down/S=8; simd 5.98-8.32x
per token               : 43 layers x 6 experts = 258 forwards, ~13.37 MB weights each
hit rate                : 57.7-67.7%; miss = SSD read 13.37MB (direct-aligned)
PREWARM                 : KILLED -29.7% (economics); persistence + pin-selection themselves work
dspark speculative      : attempts=1 drafted=4 accepted=0 -> adaptive_disabled=1 (!!)

## Scenarios (contract for EACH experiment branch)
S1 HAPPY: paired A/B on real model (frozen history, COLI_V4_SAVE_USAGE=0, gate on compressor),
   method ON vs OFF; pass = method faster with gate ok. Evidence: timing lines both arms + CSV.
S2 EDGE: token-exactness or bounded-divergence explicitly measured + stated (reduction length
   named on any parity claim). Evidence: generated_text sha comparison + probe output.
S3 REGRESSION: tiny oracle token-exact (tests/test_deepseek_v4_tiny.py) + default build
   26-object manifest unchanged + probe suite green. Evidence: exit codes + manifest diff.

## Rules learned this session (bind future experiments)
- dispatch counter or equivalent 'method actually engaged' proof REQUIRED per run
- non-engine RSS must be <= ~31GB for ram64 runs; check BEFORE launching (gate: compressor <20GB)
- rebuild validation probes from source before trusting them (stale-binary phantom failures)
- never run make deepseek-v4-tiny-check (destroys -mcpu=native binary)
- absolute tok/s not comparable across sessions; paired within-session only

## Now
6 research agents in flight (metal-ceiling, moe-accel, loading, batching-surfaces,
startup/cpu-costs, speculative/MTP). Next: collect -> plan agent -> research doc -> Oracle loop.

## Findings
(append as they land)

## Research synthesis complete (20:17) — 6 agents, key findings
CONVERGENCE: three independent streams point at the same missing middle —
  engine: target_batch batches ATTENTION up to 64 positions (:7272-7295) then drops to serial
    per-item moe_token_pipeline (:3941-3944); routing separable; coli_fp4_matmul_batch_ref
    (S=batch FP4 matmul) EXISTS UNUSED (:10660); KV dep does not block layer-level MoE batching
  upstream: llama.cpp MUL_MAT_ID needs >=32 rows/expert for mat-mat; MLX gather_qmm = one
    dispatch per gathered block, benchmark shape literally 256 experts; GPU DOES beat CPU on
    Apple Silicon decode (66-71 vs 29 t/s 7B Q4) — granularity was our killer, not the GPU
  literature: at B=1 decode NO cross-token batch exists without speculation => M1 (spec) is
    what CREATES the batch; expert skew is extreme (top 10% = 80% hits); next-layer prediction
    85-92% recall in papers; spec+cache composed +51% on GLM upstream
SLEEPERS FOUND: spec self-disable after ONE partial rejection (V4_NGRAM_PARTIAL_KEEP=1 exists);
  RoPE table rebuilt to position+1 EVERY token x layer x 3 sites; head tensors re-read per token;
  embedding row pread per token; MTP loses only on rejection-REPLAY cost at 70-89% acceptance;
  dormant block profiler hooks (:3553-3587) need glue.
DOC: performance-boost-research.md v2, 14 methods M1-M14 + 5 rejected-with-evidence.
LEAST-WORK PICK: M1 spec_keep -> branch ft-spec_keep (pending reviewer PASS).
REVIEWERS IN FLIGHT: oracle bg_9f5eeffe (technical, incl. my bandwidth-headroom claim which
  may be the weakest link: if CPU matvec is already near CPU-side bandwidth ceiling, the '100x
  headroom' framing overstates and M4's gain mechanism is weight REUSE not scheduling);
  momus bg_4311beec (plan rigor, via .omo/plans symlink).

## v3 revision + re-review (20:24)
Oracle FAIL v2 (4 blockers: bandwidth framing, RoPE-prefill wrong, M5/M6 overclaims) + Momus REJECT (QA executability). v3 fixes all + adds M15 cpu_expert_kernel (Oracle-identified gap) + per-method executable QA + resequencing. Re-submitted to both via continuation sessions. M1 harness pre-built: validation/dsv4/spec_keep_sweep.sh (5 arms x 4 prompts, engaged-counter + text_sha + gate columns).

## REVIEW PASSED (20:35) — Oracle PASS + Momus OKAY on v3. Doc committed on ft-deepmetal, pushed. Branch ft-spec_keep cut. M1 sweep launched (5 arms: base/d4k0/d4k1/d2k1/d8k1 x 4 prompts x 64 tok, ram96). Output .backlog/spec_keep_sweep.csv + log.
